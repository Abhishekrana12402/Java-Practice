package Interface.ExtendDemo;

public interface A {
    void fun();
    //static interface methods must always have a body as they cant be overridden means cant be inherited , Inheritance leads to creation of an object and static methods dont depend on objects
}
