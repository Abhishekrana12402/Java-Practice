import java.util.Scanner;public class Basics {    /*    a=10 a is  reference variable which is stored in  a stack and 10 will is an object stored in heap memory    multiple references can point to same object , if any  references change the object then original object is changed    All the objects that don't have any reference variables will be removed from the memory when garbage collection hits    How java code executes?    ->It is written in .java file(human-readable).it is a source code    -> Then compiling it into .class file (byte code) we need jvm to run this    Platform Independence: Means that byte code can run in any operating system .C,C++ leads to .exe file which  is platform dependent    (will not run on different operating system)    ->JVM which is required to run the program is platform dependent    JDK(java development kit):It provides an environment to develop and run the program    It is a package that includes:    development tools    jre(java run time environment)-to execute the program    compiler    interpreter/loader    JRE(java runtime environment):it is an installation that provides an environment to only run the program    It consists of :Deployment technologies    user interface toolkits    integration libraries    base libraries    jvm    static variables do not depend on object of classes    jvm contains the stack and heap memory allocations    Java source code->JDK->Byte Code->JVM->JRE    Interpreter ::    line by line execution .    when one method is called several times then it will interpret again and again    Just-in-Time Compiler improves the efficiency . It does not interprets it again and againJvm is a machine or subset of jre(jvm+some extra files or functionalities)Packages are folder in which java file lies     */    Scanner in=new Scanner(System.in);    //Scanner is a class which allows us to take input    int a=in.nextInt();    /*    Primitive data types are data types that cannot be further divided into any further data types    int,char,float,double,long,boolean    primitive data types are predefined by java whereas non-primitive  are created by programmer and they can    be further divided into smaller data types Eg:String->character    int a= 60  here 60 is referenced as literals and a as identifier    Type Conversion:    Destination data type should be greater than source data type    eg:int num=input.nextFloat();    this will give an error since float>int and our destination or required type is smaller than the input type    But we can convert the bigger type into smaller by type casting eg:int num=(int)(34.5);    Type promotions : Maximum value we can store in the bytes is 256    Suppose we have byte a=40, byte b=50 and we want int c=a*b which is 2000 and byte*byte=byte but since answer exceeds    256 hence java promotes the byte into int this is known as type promotion     */    public static void main(String[] args) {        byte c = 40;        byte d = 50;        byte e = (byte) (c * d);        System.out.println(e);//        System.out.println(3*'a');//        byte b=42;//        char c='a';//        short s=1024;//        int i=5000;//        float f=5.67f;//        double d =0.1234;//        double result=(f*b)+(i/c)+(d*s);//        System.out.println((f*b)+" "+(i/c)+" "+(d*s));//        System.out.println(result);        // float*byte expression is converted into float since it is bigger        //int/char is converted into integer        //double *short is converted into double and final expression float+int+double will result into double    }    /*    Java supports unicode system(Unicode is a universal international standard character encoding that is capable of representing most of the world's written languages.)     */}