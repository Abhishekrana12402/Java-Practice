public class Types_of_languages {
   /* Procedural: specifies a series of well-structured steps and procedures to compose a program
    functional:writing only in pure function i.e. never modify variables,but only create new ones as a output
    Used in a situation were we have to perform lots of different operations on the same dataset
    Object-Oriented Programming:Have templates which is known as classes and their instances as object

    Static vs Dynamic Languages
    static:performs type checking at compile time
    Errors will show at compile time
    Declare datatype before we use it
    Dynamic:performs checking at runtime
    Error might not show till program is run
    No need to declare datatype of variables
    */
}
